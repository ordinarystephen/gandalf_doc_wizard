# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# Environment — never commit real credentials
.env

# Runtime data (FAISS indexes, SQLite log, uploaded source files — too large for git)
data/*.faiss
data/*.json
data/*.db
data/sources/

# Legacy HuggingFace model cache — not used on overhaul/domino, kept out of git
models/

# OS
.DS_Store
